============
Installation
============

At the command line::

    $ easy_install django-exporter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-exporter
    $ pip install django-exporter
